import { getPathName } from "@/utils/utils";
import { menuItems } from "../layout";
import { PageHeader } from "@/components/UI/Header/PageHeader";

export default function About() {
    return <div>  <PageHeader title='About Me' /></div>
}